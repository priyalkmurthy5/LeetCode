class Solution:
    def isPalindrome(self, x: int) -> bool:
        num_str = str(x)
        rev_num = num_str[::-1]
        if rev_num == num_str:
            return True
        else:
            return False
