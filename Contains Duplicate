class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        create_set = set(nums)
        if len(nums) == len(create_set):
            return False
        else:
            return True              
